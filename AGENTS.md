# AGENTS.md

This project's full instructions live in [`CLAUDE.md`](./CLAUDE.md). Read it before working — it contains the persistent workflow, architecture, commands, DevTools MCP/isolated-profile security setup, and skill-loading guidance.

- Both Kilo and Claude Code auto-load this file and `CLAUDE.md`, so keep all project guidance in `CLAUDE.md` to avoid duplication. When adding project instructions, edit `CLAUDE.md`, not this file.
